"use client";
import { useState } from "react";
import Link from "next/link";
import { skillQuestions } from "@/data/library";
export function SkillCheck(){const [idx,setIdx]=useState(0),[scores,setScores]=useState<number[]>([]); const done=idx===skillQuestions.length; const score=scores.reduce((a,b)=>a+b,0); const result=score<7?["Beginner","Mulai dari Roadmap Level 0–1","AI Fundamentals","Personal Prompt Library"]:score<14?["AI Operator","Fokus Level 2–3","Prompting That Actually Works","Weekly Work Copilot"]:score<20?["Workflow Builder","Fokus Level 4","Research & Content Workflow","AI Content Creation System"]:score<26?["Automation Builder","Fokus Level 5–6","API, Webhook, and Data Flow","AI Lead Management"]:["Agentic Builder","Fokus Level 7–9","AI Agents & Orchestration","Research Agent Terukur"];
 if(done)return <div className="glass card" style={{padding:36}}><span className="eyebrow"><span className="dot"/>Hasilmu</span><div className="metric blue" style={{marginTop:24}}>{result[0]}</div><p>{result[1]}. Modul yang paling pas buatmu: <strong style={{color:"white"}}>{result[2]}</strong>.</p><ul className="list"><li>Skor: {score} dari {skillQuestions.length*3}</li><li>Project pertama: {result[3]}</li><li>Coba lagi setelah kamu menyelesaikan 2–3 project.</li></ul><div style={{display:"flex",gap:10,marginTop:24,flexWrap:"wrap"}}><Link href="/roadmap" className="btn btn-primary">Lihat roadmap</Link><button className="btn" onClick={()=>{setIdx(0);setScores([])}}>Ulangi</button></div></div>;
 const q=skillQuestions[idx];return <div className="glass card" style={{padding:36}}><div className="progress"><span style={{width:`${idx/skillQuestions.length*100}%`}}/></div><div className="card-top" style={{marginTop:24}}><span>Pertanyaan {idx+1}/{skillQuestions.length}</span><span>Jawab apa adanya</span></div><h2 style={{fontSize:32,margin:"24px 0"}}>{q.question}</h2>{q.options.map(o=><button className="quiz-option" key={o.label} onClick={()=>{setScores([...scores,o.score]);setIdx(idx+1)}}>{o.label}</button>)}</div>}
