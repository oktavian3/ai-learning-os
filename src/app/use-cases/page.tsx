import { PageHero } from "@/components/Primitives";import { ResourceExplorer } from "@/components/ResourceExplorer";import { useCases } from "@/data/library";
export const metadata={title:"Use Cases"};export default function Page(){return <><PageHero label="88 contoh yang konkret" title="AI bisa dipakai buat apa aja?" description="Bukan buat ‘membuat semuanya otomatis’. Lebih sering terpakai untuk bagian kerja yang paling menyulitkan: riset, membersihkan data, membuat draft, memeriksa, dan mindahin informasi."/><section className="section"><div className="container"><ResourceExplorer items={useCases} basePath="/use-cases"/></div></section></>}
