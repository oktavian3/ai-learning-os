import { PageHero } from "@/components/Primitives";import { ResourceExplorer } from "@/components/ResourceExplorer";import { workflows } from "@/data/library";
export const metadata={title:"Workflow Library"};export default function Page(){return <><PageHero label="10 workflow siap bongkar" title="Workflow beneran — dari masalah sampai output." description="Jalankan manual dulu. Kalau hasilnya sudah konsisten, baru otomasi. Biar gak bikin mesin error yang lebih cepat."/><section className="section"><div className="container"><ResourceExplorer items={workflows} basePath="/workflows"/></div></section></>}
