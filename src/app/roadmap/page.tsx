import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PageHero,CTA } from "@/components/Primitives";
import { roadmap } from "@/data/library";
export const metadata={title:"Roadmap"};
export default function Roadmap(){return <><PageHero label="10 level · dari nol sampai agent" title="Ada urutannya. Tidak perlu buru-buru." description="Mulai dari dasar. Pakai buat kerjaan nyata. Baru masuk ke workflow, automation, RAG, dan agent."/><section className="section glow-section"><div className="container timeline"><div style={{marginBottom:24,display:"flex",alignItems:"center",gap:8,fontSize:13,color:"#82b6ff"}}><span className="dot"/>Klik level untuk mulai belajar</div>{roadmap.map(r=><Link key={r.level} href={`/course/${String(r.level).padStart(2,"0")}`} style={{textDecoration:"none",color:"inherit"}}><article className="glass timeline-card" style={{cursor:"pointer"}}><div className="level-num">{String(r.level).padStart(2,"0")}</div><div><span className="pill">Pas buat: {r.forWho}</span><h3>{r.title}</h3><p>{r.learn}</p><ul className="list"><li>Kalau beres: {r.output}</li><li>Project-nya: {r.project}</li></ul></div><div className="timeline-meta"><span>{r.lessons}</span><span>Tools: {r.tools.join(" · ")}</span><span className="blue" style={{display:"flex",alignItems:"center",gap:6}}>Mulai level ini <ArrowRight size={13}/></span></div></article></Link>)}</div></section><CTA/></>}